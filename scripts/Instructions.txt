Instructions:

Get a new token from https://developers.facebook.com/tools/explorer
bundle exec ruby scripts/albumscrape.rb [token]
./cli pull-beer-info
./cli update-ids --source-file [PATH]
Set beer ids in js/extra.json

-------

./cli pull-beer-info
ruby scripts/stats.rb && ./cli get-breweries

./cli pull-images && ruby scripts/distributeBreweries.rb && ./cli create-posts

------


Update beers.html with the beer count

git add -A && git commit -m "Update beers" && git push

git add -A && git commit -m "Update beer count" && git push